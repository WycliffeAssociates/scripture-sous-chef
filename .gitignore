/target
/corpora
/data
/ebible-main
/debug
# .claude/ holds worktrees, local settings, and session state that must
# never be tracked — except skills/, which is repo-shared tooling knowledge
# (see .claude/skills/oracle-gate/SKILL.md) meant to travel with the repo.
/.claude/*
!/.claude/skills/
notes.md
.repomix
/experiments
profile.json.gz
*.profile.json
*.profile.json.gz
.codegraph/

# macOS finder cruft
.DS_Store